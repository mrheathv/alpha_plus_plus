/// **A namespace, since M8.** This drew a `BuildingMassing` as SpriteKit
/// shape nodes: which faces were visible, how each was lit, the order they
/// were painted in. Metal draws the massing as geometry now
/// (`MetalCityMesh.building`). What outlives it is the close-up marks derived
/// from a building's panels, in `NearDetail.swift`.
enum IsometricBuilding {}
